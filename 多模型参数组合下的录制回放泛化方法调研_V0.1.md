# 多模型参数组合下的录制回放泛化方法调研（V0.1）

> 日期：2026-07-29  
> 范围：以大模型推理的算子/子图录制回放为主，兼顾后续训练扩展；重点考察模型尺寸、量化格式、并行方案、机器规模、单卡显存、请求值与运行时状态对 shape、数值、控制路径和性能的影响。  
> 目标模型：DeepSeek-V4 Pro/Flash、GLM-5.2、Qwen3.8、MiniMax-M3。  
> 与前序材料的关系：本文继承仓库既有的 `Execution Regime`、`PathFingerprint`、Path-Preserving/Path-Exploring Replay 等结论，新增多模型事实核验、逻辑/物理 shape 分层、量化与拓扑映射、模型适配器方案及第一版验证矩阵。  
> 证据截止：2026-07-29。模型、框架和昇腾支持状态变化很快，文中结论必须与链接、checkpoint revision 和软件版本一起使用。

## 1. 执行摘要

### 1.1 第一版核心结论

不同模型参数组合的泛化对象不能定义成“一个新 shape”，也不能只定义成 `(op, shape, dtype)`。更合适的对象是一个可审计的**执行域**：

```text
ExecutionRegime =
    ModelStructure
  × CheckpointAndQuantization
  × ParallelPlanAndPlacement
  × HardwareTopologyAndCapacity
  × WorkloadAndInputValues
  × RuntimeState
  × CompilerKernelAndSoftwareVersion
```

录制回放至少需要同时泛化并验证五件事：

1. **逻辑 shape**：模型语义中的 token、hidden、head、expert 等维度；
2. **物理 shape/layout**：分片、padding、量化打包、scale、KV page、图桶和通信 buffer 的实际形状；
3. **数值决策**：MoE 路由、稀疏注意力 TopK、MTP 接受数、EOS、cache 命中等；
4. **控制与状态路径**：模型分支、编译 Guard、Kernel/Tiling、调度、缓存、通信与负载均衡路径；
5. **性能证据域**：该样本是同路径实测、同路径插值、跨域外推，还是未知。

由此得到四个直接判断：

- **模型大小变化不是统一比例缩放。** DeepSeek-V4 Pro 与 Flash 同属一族，但 hidden、层数、专家数、稀疏 TopK 和层类型排布均不同，不能用一个参数量比例缩放 Flash 轨迹得到 Pro 轨迹。
- **量化不是仅改变 dtype 和显存。** 它还会改变权重打包 shape、scale tensor、激活量化/归约、Kernel、对齐、workspace，甚至 checkpoint 自带的 MTP 配置；量化误差还可能改变 TopK/路由/接受判断，造成后续路径级联变化。
- **卡数和显存不是平滑自变量。** 它们先决定某个并行方案、图捕获规格、KV 容量、调度上限、是否 offload/重算，然后才决定局部算子 shape 和通信。64 卡到 128 卡既可能只是 DP 副本翻倍，也可能是 TP/EP/CP/PP 重分解，两者完全不是同一种泛化。
- **性能只允许在可证明的等价执行域内复用。** 数值看似正确但路径不同，或逻辑 shape 相同但物理布局、通信域、Kernel/Tiling 不同，都不能给出高置信度的目标性能结论。

### 1.2 建议采用的总体方法

第一版推荐落地成三层：

```text
层 1：ExecutionSpec
  规范化模型、checkpoint、量化、并行、硬件、工作负载、状态和软件版本

层 2：ModelAdapter + Symbolic Shape IR
  把 ExecutionSpec 编译成逻辑 shape、物理 shape、合法性约束和候选路径谓词

层 3：Observed PathFingerprint
  在真实硬件上采集模型/图/Kernel/调度/状态/通信实际路径，并修正预测
```

这不是试图一次得到覆盖所有模型的闭式公式，而是建立：

- 可迁移的公共 IR；
- 每个架构少量、显式、可版本化的 Adapter；
- 基于边界和路径覆盖的实验生成器；
- 发现未知路径后自动补测的闭环。

### 1.3 本次调研的重要证据边界

截至 2026-07-29：

- DeepSeek-V4 Pro/Flash、GLM-5.2、MiniMax-M3 均可取得官方开放权重或官方 checkpoint 配置；
- 官方 QwenCloud 文档只列出了 `qwen3.8-max-preview` 云端预览型号，确认 1M 上下文，但本次没有在 Qwen 官方开放权重列表中找到可审计的 Qwen3.8 checkpoint/config。因此本文不猜测其 hidden size、层数、MoE 和 attention 结构，只给出接入策略和待开放后的验证项。[QwenCloud 官方模型列表](https://docs.qwencloud.com/developer-guides/getting-started/text-generation-models)、[Qwen 官方 Hugging Face 组织](https://huggingface.co/Qwen)

这意味着“目标名单里有某型号”与“已经具备算子级录制回放研究条件”必须分开管理。

## 2. 问题定义：究竟有哪些东西会变

### 2.1 四类 shape 必须分开

| 类型 | 示例 | 为什么重要 |
|---|---|---|
| 语义/逻辑 shape | `[T,H]`、`[T,Nq,Dh]`、router `[T,E]` | 描述模型数学语义，适合跨后端迁移 |
| 分片后局部 shape | `[T,H/TP]`、本地 expert 数、local KV head 数 | 决定每个 rank 的计算和通信 |
| 物理存储 shape | INT4 packed、FP8/MXFP8 scale、C8/NZ layout、KV page | 直接决定 Kernel、内存、对齐和回放 buffer |
| 执行 shape | graph bucket padding 后的 token 数、capacity padded expert tokens、通信 payload bucket | 才是性能测试实际执行的尺寸 |

一个典型错误是只记录逻辑 `[T,H]`，回放时却不知道原执行使用了：

- `[T_pad,H_local]` 的图桶；
- `[out, in/2]` 的 4 bit packed 权重；
- `[ceil(out/g_o), ceil(in/g_i)]` 的 scale；
- 按专家动态分组的 ragged token 列表；
- 跨节点 AllToAllV 的不均匀 send counts。

### 2.2 三类“结果”也必须分开

| 结果层 | 例子 | 回放要求 |
|---|---|---|
| 连续数值 | logits、hidden states、attention output | 按 dtype/量化定义容差或误差分布 |
| 离散决策 | TopK expert、稀疏 block、MTP accepted tokens、EOS | 原则上要求精确匹配，否则路径可能改变 |
| 状态转移 | KV append、prefix hit、block allocate/evict、EPLB remap | 要验证转移前状态、事件和转移后状态 |

量化导致 logits 小幅变化可能仍满足连续误差容限，却可能让第 `K` 与第 `K+1` 个 expert 交换，进而改变 AllToAllV、expert GEMM shape 和后续数值。对于录制回放，离散决策通常比平均误差更关键。

### 2.3 本版优先研究推理，训练需额外增加状态维度

本文优先研究 Prefill、Decode、MTP、KV Cache 和服务调度，因为目标模型的官方部署资料和当前 PoC 都集中在推理。训练泛化还需额外加入：

- micro batch、global batch、gradient accumulation；
- optimizer/ZeRO/FSDP shard；
- activation checkpoint/recompute；
- loss scaling、overflow/skip step；
- 梯度归约顺序和 optimizer state；
- MoE auxiliary loss 与 capacity/drop 策略。

公共的 shape、路径和拓扑方法可以复用，但训练不能直接套用推理的状态机。

## 3. 目标模型事实核验与风险地图

### 3.1 证据分级

本文按以下顺序使用资料：

1. **A 级**：官方 checkpoint、`config.json`、官方模型卡/技术报告；
2. **B 级**：vLLM、vLLM Ascend、Transformers 等官方实现和部署文档；
3. **C 级**：官方仓库 issue/roadmap，仅用于说明实现状态和待验证问题；
4. **未证实**：新闻、社区传言或没有开放配置的型号，不用于推导精确 shape。

### 3.2 模型结构对照

| 模型 | 已核验结构摘要 | 首要路径风险 | 证据 |
|---|---|---|---|
| DeepSeek-V4-Pro | 1.6T total / 49B activated；`H=7168`，61 个主层，384 routed experts、Top-6；主层为 30 个 C4A + 31 个 C128A；1M context；Instruct 权重为 expert FP4、其余大多 FP8 | C4/C128 压缩边界、稀疏 index TopK、滑窗、mHC、MoE 路由、MTP、混合 KV cache | [模型卡](https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro)、[Pro config](https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro/raw/main/config.json) |
| DeepSeek-V4-Flash | 284B total / 13B activated；`H=4096`，43 个主层，256 experts、Top-6；主层配置含 2 个无压缩/SWA 路径、21 个 C4A、20 个 C128A；1M context；同样为 expert FP4 + 其余 FP8 的 Instruct 权重 | 与 Pro 不是等比结构；较小 `index_topk=512`；层类型排布和本地 expert 分片不同 | [模型卡](https://huggingface.co/deepseek-ai/DeepSeek-V4-Flash)、[Flash config](https://huggingface.co/deepseek-ai/DeepSeek-V4-Flash/raw/main/config.json) |
| GLM-5.2 | 官方 HF 页面标为 753B BF16；`H=6144`，78 层；前 3 层 dense、后 75 层 sparse MoE；256 experts、Top-8；DSA `index_topk=2048`；IndexShare 每 4 层共享 indexer，配置中 21 个 `full`、57 个 `shared`；1M context | full/shared indexer 路径、DSA TopK、dense/MoE 分界、MTP 接受、W4A8C8/W8A8/BF16 差异 | [模型卡](https://huggingface.co/zai-org/GLM-5.2)、[config](https://huggingface.co/zai-org/GLM-5.2/raw/main/config.json) |
| Qwen3.8 | 官方云文档目前仅确认 `qwen3.8-max-preview`、1M context、thinking/function calling 等 API 能力；未发现官方开放 config | 无法审计模型算子、shape 和内部控制路径；只能做黑盒 API 行为记录，不能做可信算子回放 | [官方云模型列表](https://docs.qwencloud.com/developer-guides/getting-started/text-generation-models) |
| MiniMax-M3 | 原生多模态 MoE，约 428B total / 23B activated；`H=6144`，60 层；前 3 层 dense/非稀疏，后 57 层 MoE + MSA；128 experts、Top-4；MSA block=128、Top-16 blocks；64 Q heads、4 KV heads；1M context | MSA block 选择、MoE route、text/image/video 分支、动态分辨率、MTP、量化 checkpoint 配置漂移 | [模型卡](https://huggingface.co/MiniMaxAI/MiniMax-M3)、[BF16 config](https://huggingface.co/MiniMaxAI/MiniMax-M3/raw/main/config.json) |

说明：

- DeepSeek-V4 的层类型统计按 `num_hidden_layers` 对 `compress_ratios` 的实际实现映射计算。vLLM 代码会对主层把配置中的 `0` 映射为无压缩路径，并对 MTP 单独处理；因此不能假设“层配置数组长度一定等于主层数”。[vLLM DeepSeek-V4 attention 实现](https://docs.vllm.ai/en/latest/api/vllm/models/deepseek_v4/attention/)
- MiniMax 官方 BF16 config 当前是 `num_mtp_modules=7`，而官方 MXFP8 checkpoint 的 config 当前为 `num_mtp_modules=1`。这可能来自 checkpoint 发布/版本差异，但已经足以证明：**量化变体接入时必须 diff 全量 config，不能只替换 dtype。**[MiniMax-M3-MXFP8 config](https://huggingface.co/MiniMaxAI/MiniMax-M3-MXFP8/blob/main/config.json)

### 3.3 DeepSeek-V4：最典型的“同模型族、多个状态机”案例

DeepSeek-V4 的长上下文路径至少包含：

- C4A：近似 4 倍压缩，压缩窗口存在 overlap；
- C128A：128 倍压缩；
- 短滑窗的未压缩本地注意力；
- C4A 稀疏 indexer TopK 与 C128A 近全量压缩注意力；
- compressor residual/state cache；
- FP4 indexer cache、FP8 attention cache 等物理布局；
- MoE dispatch/combine；
- MTP 草稿与验证。

官方 vLLM 说明使用统一的 256 个 native token 作为逻辑 block；C4A block 物理保存 64 个压缩项，C128A block 只保存 2 个。相同原始 context length 在不同层会形成不同的 KV 物理 shape，但 scheduler、prefix cache 又以统一逻辑 block 记账。[vLLM DeepSeek-V4 长上下文实现说明](https://github.com/vllm-project/vllm-project.github.io/blob/main/_posts/2026-04-24-deepseek-v4.md)

实际实现还有明确的值/位置条件：

```text
if (position + 1) % COMPRESS_RATIO != 0:
    return
```

即大量 token 只更新 partial state，不产生新的 compressed KV entry；当位置跨过 4/128 边界时，算子序列、写缓存行为和输出 shape 才改变。[vLLM fused compressor 源码文档](https://docs.vllm.ai/en/latest/api/vllm/models/deepseek_v4/common/ops/fused_compress_quant_cache/)

因此首批必须扫描：

- `position mod 4` 与 `position mod 128`；
- logical KV block 边界前/等于/后；
- `available_compressed_tokens` 小于/等于/大于 TopK；
- Prefill/Decode 混合；
- IndexCache compute/reuse；
- MTP accepted token 数；
- 路由均匀、热点和极端偏斜。

### 3.4 GLM-5.2：跨层共享本身就是控制路径

GLM-5.2 不能只记录一条“DSA attention”：

- 前 3 层是 dense MLP，后 75 层是 MoE；
- `indexer_types` 在 `full/shared` 间切换；
- `index_topk_freq=4`，共享 indexer 意味着部分层计算 index，其他层消费之前的选择；
- MTP 可以共享 index，且接受长度会改变验证 step；
- Ascend 部署还可切换 `enable_dsa_cp`、`enable_sparse_sfa_c8`、`enable_sparse_li_c8`、MC²、图模式等。

官方 vLLM Ascend 文档给出的 GLM-5.2 量化变体包括 W8A8 和 W4A8C8。其硬件门槛已经显示量化会改变可行部署域：

- BF16：至少 2 个 `128G × 8` A3 节点或 4 个 `64G × 8` A2 节点；
- W8A8/W4A8C8：1 个 `128G × 8` A3 节点或 2 个 `64G × 8` A2 节点。

同一模型因量化/显存不同可以从 32 卡降到 8/16 卡，随后 TP、DP、EP、通信域和 KV 容量均可能改变。[vLLM Ascend GLM-5.2 部署文档](https://docs.vllm.ai/projects/ascend/en/latest/tutorials/models/GLM5.2.html)

### 3.5 MiniMax-M3：稀疏、MoE、多模态三种动态性叠加

MiniMax-M3 的配置直接暴露了三类 shape 来源：

1. **MSA**：block size=128，每个 query 选择 Top-16 blocks；
2. **MoE**：128 experts、Top-4，每个 expert 的 token 数是输入值相关的 ragged shape；
3. **多模态**：视觉塔 32 层、patch size=14、动态分辨率网格、patch merge、视频帧分段。

文本、单图、多图和视频不是在 `[T,H]` 上简单改变 `T`：

- 是否执行 vision tower 是模型级分支；
- 原图尺寸经过动态 resize/tile 后产生不同视觉 token 数；
- 图像/视频 placeholder 插入改变文本位置和 attention mask；
- 多模态 projector 产生额外算子和中间 shape；
- 视觉 token 再进入 MSA/MoE，改变 block 数和 expert route。

vLLM 的 M3 metadata 也显式拆分 Prefill 与 Decode，并记录 `num_actual_tokens`、`total_kv_blocks`、`cu_seqlens` 等 ragged 信息，说明仅用 batch max length 无法复现实际执行。[vLLM MiniMax-M3 sparse metadata](https://docs.vllm.ai/en/latest/api/vllm/models/minimax_m3/common/sparse_attention/)

当前 vLLM 官方 recipe 验证了 NVIDIA/AMD 部署，vLLM Ascend 支持矩阵尚未列出 MiniMax-M3。因此在昇腾录制回放中应先做“算子/架构可用性核验”，不能默认与 MiniMax-M2 兼容。[vLLM MiniMax-M3 recipe](https://recipes.vllm.ai/MiniMaxAI/MiniMax-M3)、[vLLM Ascend 支持矩阵](https://docs.vllm.ai/projects/ascend/en/latest/user_guide/support_matrix/supported_models.html)

### 3.6 Qwen3.8：暂时作为“证据缺失域”处理

Qwen3.8 当前可做：

- 记录 API 输入、输出 token、thinking 开关/预算、停止原因、时延；
- 建立版本监控，发现官方 config/checkpoint 后触发 Adapter 接入；
- 把已有 Qwen3.5/3.6 Adapter 作为候选模板，但不声明结构相同。

当前不可做：

- 推导精确 QKV/MLP/MoE shape；
- 断言 GDN/attention/MTP 层排布；
- 用 API 时延反推单算子性能；
- 声称完成开源权重的路径保持回放。

系统应把此类对象标成：

```text
evidence_level = api_only
shape_confidence = unknown
operator_replay_eligible = false
```

## 4. 参数如何传导到 shape、结果和路径

### 4.1 参数因果链

```text
模型/Checkpoint
  ├─ 结构常量：H、L、heads、experts、layer schedule
  ├─ 量化布局：packed weights、scale、ignored layers、KV dtype
  └─ tokenizer/chat template/MTP head
          ↓
并行与放置编译
  ├─ TP/PP/DP/EP/CP/DCP
  ├─ 每层/每权重/每 expert 所在 rank
  └─ 通信 group 与跨机边界
          ↓
硬件容量约束
  ├─ 可用 HBM、图缓存、workspace、通信 buffer
  └─ max tokens、max seqs、block 数、offload/recompute
          ↓
请求值与运行时状态
  ├─ ragged lengths、route、TopK、prefix hit、MTP accept
  └─ scheduler batch、cache 水位、EPLB 映射
          ↓
图/Kernel/Tiling 与实际性能
```

### 4.2 参数影响表

| 参数族 | 逻辑 shape | 物理 shape/layout | 数值/路径 | 系统性能 |
|---|---|---|---|---|
| 模型尺寸/结构 | H、L、head、FFN、E、TopK | 权重块、层排布 | attention/MoE/混合层路径 | FLOPs、权重带宽、cache |
| 量化 | 通常数学 shape 不变 | packed、scale、zero-point、对齐、C8/NZ | rounding 改变 route/TopK/MTP；Kernel 改变 | 容量、带宽、量化开销 |
| TP | local head/hidden/weight shard | local buffer 与 collective buffer | head 复制/分片、collective | 算通比、跨机通信 |
| EP | local expert 集合 | dispatch ragged buffer | route 目的 rank、EPLB remap | AllToAllV、负载长尾 |
| PP | 每 stage 层集合 | stage activation buffer | stage 边界、bubble | P2P、流水调度 |
| DP | 单副本数学结构通常不变 | 每副本独立 cache | 请求分配、MoE 场景下 DP×TP EP group | 吞吐、batch composition |
| CP/DCP | local sequence/KV shard | KV interleave/page | attention gather/reduce | 长上下文容量与通信 |
| 卡数/显存 | 间接影响 | 决定可行分片、cache/graph 数 | 触发方案、offload、重算切换 | 离散跳变而非平滑缩放 |
| 请求 shape | T、B、S、图像 token | bucket padding、block/table | Prefill/Decode/chunk/vision | Kernel/图、cache、调度 |
| 请求值 | 通常输入 shape 相同 | ragged route/index buffer | expert、sparse block、EOS | 动态负载与执行量 |
| 状态 | 可能相同 | cache pages、partial state | hit/miss/evict/recompute | 跳过计算或额外搬运 |
| 软件/编译 | 数学 shape 可相同 | fusion/format/workspace | graph Guard、fallback、TilingKey | 可能完全不同 |

## 5. 第一版 Symbolic Shape IR

### 5.1 公共符号

```text
B       : scheduled sequences
T       : 本 step 的有效 query token 总数
S_i     : 第 i 个请求的 context/sequence length
H       : hidden size
L       : main layer count
Nq/Nkv : query/KV head count
Dh      : head dimension
I       : FFN intermediate size
V       : vocab size
E       : routed expert count
K       : experts per token
P_t     : tensor parallel size
P_e     : expert parallel size
P_c     : context/decode-context parallel size
P_p     : pipeline parallel size
b       : KV/cache/attention block size
G_w     : quant weight block/group
C_card  : 单卡可用显存
```

### 5.2 Dense/GQA 的基础形状

对于普通 GQA/MHA 层，候选逻辑表达式为：

```text
hidden              = [T, H]
q_global            = [T, Nq,  Dh]
k/v_global          = [T, Nkv, Dh]
q_local(TP head)    = [T, Nq / P_t, Dh]
k/v_local           = [T, Nkv / P_t, Dh]  # 仅当可整除且实现选择分片
mlp_up_local         = [T, I / P_t]
lm_head_local        = [T, V / P_t]
```

但 Adapter 必须处理三种例外：

- `Nkv < P_t` 时 KV head 可能复制而不是继续切分；
- shard 维度不可整除时可能 pad、拒绝配置或换一种并行；
- MLA、DeepSeek-V4 压缩注意力不能套用标准 GQA 公式，应使用 `q_lora_rank`、`kv/head rank`、compressor 和实现的专用公式。

因此 IR 不只支持除法，还必须支持：

```text
ExactDiv(x,p)
CeilDiv(x,p)
AlignUp(x,a)
Replicate(x,p)
Min/Max
Piecewise(predicate, a, b)
```

### 5.3 MoE 的 ragged shape

MoE 的关键不是 router `[T,E]`，而是每个 expert 实际收到多少 token：

```text
router_logits = [T, E]
topk_indices  = [T, K]
n_e           = count((token,slot) routed to expert e)
sum_e(n_e)    = T × K
```

对 EP rank `r`：

```text
local_experts(r) = placement_map[r]
recv_tokens(r)   = sum(n_e for e in local_experts(r))
expert_input(e)  = [n_e, H]
```

`n_e` 是输入值、量化、batch composition 和 router 实现共同决定的运行时变量。若实现使用 capacity/padding：

```text
capacity = AlignUp(CeilDiv(capacity_factor × T × K, E), tile)
expert_input_physical = [num_local_experts, capacity, H_local]
```

否则通常是 grouped/ragged GEMM。录制系统必须保存：

- `n_e` histogram；
- token→expert 和 expert→rank 映射；
- per-peer send/recv counts；
- drop/pad 数；
- EPLB 前后 placement map；
- grouped GEMM 的 group offsets。

只保存平均 expert token 数会丢失性能最关键的最大负载和 AllToAllV 不均衡。

### 5.4 稀疏/压缩注意力的 piecewise shape

DeepSeek-V4 不应把压缩 token 数简单写死为 `ceil(S/r)`，因为还有 overlap、partial state、因果边界和 block/page 语义。第一版 IR 应表示为：

```text
compressed_entries =
  CompressorCount(
    native_length=S,
    ratio=r,
    overlap=(r == 4),
    prior_partial_state,
    implementation_revision
  )

selected_entries = min(index_topk, eligible_compressed_entries)
```

同时记录：

```text
position_mod_ratio
partial_state_valid_tokens
logical_block_id
compressed_slot_mapping_shape
topk_index_shape
```

MiniMax-M3 的 MSA 可先表示为：

```text
kv_blocks_i      = CeilDiv(S_i, 128)
selected_blocks  = min(16, eligible_blocks_i) + required_local/init_blocks
```

但“required local/init blocks 是否去重、实际 CSR offsets 和 causal mask”必须从真实实现/metadata 获取，不由通式猜测。

GLM-5.2 则需要在 shape 之外增加：

```text
indexer_action(layer) = full_compute | shared_reuse
selected_tokens       = min(2048, eligible_tokens)
```

### 5.5 量化物理 shape

以常见模式为例：

```text
W4 packed weight:
  logical  = [O, I]
  packed   = [O, CeilDiv(I, 2)] uint8
  scales   = [O or CeilDiv(O,G_o), CeilDiv(I,G_i)]

block FP8:
  logical  = [O, I]
  data     = [O_pad, I_pad] fp8
  scales   = [CeilDiv(O,G_o), CeilDiv(I,G_i)]

dynamic activation quant:
  input    = [T, I]
  q_input  = [T_pad, I_pad]
  scales   = implementation-specific [T,...] or [T,CeilDiv(I,G)]
```

需要把以下字段纳入 shape signature：

- quant method 与版本；
- W/A/KV 各自位宽和数据格式；
- symmetric/asymmetric、static/dynamic；
- group/block shape；
- scale/zero-point dtype 与 shape；
- ignored layer 列表；
- pad/pack order；
- dequant/fused kernel；
- calibration/checkpoint hash。

vLLM 当前支持多种 FP8、INT8、INT4、AWQ、GPTQ 等格式，并且不同硬件支持矩阵不同；所以“W4A8”也不能脱离具体实现名和硬件理解。[vLLM Quantization 文档](https://docs.vllm.ai/en/stable/features/quantization/index.html)

### 5.6 图桶导致的执行 shape

图模式通常不能直接对任意 token 数重放。Ascend ACLGraph 会捕获多个 shape，再把输入 pad 到最近的图桶：

```text
T_exec = min(bucket in captured_buckets where bucket >= T_actual)
padding_tokens = T_exec - T_actual
```

因此相同 `T_actual` 在不同 `capture_sizes` 配置下可能执行不同 shape。Prefill 和 Decode 混合、MLA token 计算方式不同还会造成不同图路径。[vLLM Ascend ACLGraph 说明](https://docs.vllm.ai/projects/ascend/en/latest/developer_guide/feature_guide/ACL_Graph.html)

必须记录：

- captured bucket 列表；
- 命中 bucket；
- actual/padded token 数；
- graph ID；
- Guard、recompile/fallback；
- graph mode 与 compile cache 状态。

## 6. 机器规模与显存：先编译并行方案，再谈 shape

### 6.1 不能只记录 `world_size`

至少要记录完整 rank-group 图：

```text
ParallelPlan
  attention: TP/DP/CP/DCP
  dense_mlp: TP
  moe: TP or EP or DP×TP expert group
  pipeline: PP stage map
  serving: mixed or P/D disaggregated
  layer_sharding / fine-grained TP overrides
  rank -> node/device/die/link placement
```

EP 在不同框架中可能与 DP/TP group 叠加，不能简单要求：

```text
world_size == TP × DP × PP × EP × CP
```

例如 vLLM 的 MoE DP 部署中，attention 可按 DP 复制，而 expert 层跨 `(DP × TP)` 组成 expert group。[vLLM Data Parallel Deployment](https://docs.vllm.ai/en/v0.12.0/serving/data_parallel_deployment/)

### 6.2 64 卡与 128 卡的三种完全不同解释

假设都是每机 8 卡：

| 方案 | 64 卡（8×8）→128 卡（16×8） | 局部算子 shape | 路径/性能变化 |
|---|---|---|---|
| A：固定 TP=8，DP 8→16 | 每副本仍在单机，副本翻倍 | 大部分模型算子局部 shape 不变 | 请求分配、batch、EP group/调度、全局吞吐变化 |
| B：TP 8→16 | 一个副本跨 2 机，DP 可不变 | head/hidden/weight shard 可能减半 | collective 跨机，算法/带宽/延迟改变 |
| C：增加 PP/CP/PD 节点 | 将长上下文或 Prefill/Decode 拆开 | layer/sequence/KV local shape 改变 | P2P、KV transfer、pipeline/PD scheduler 新路径 |

若只把 `world_size=64/128` 输入一个回归模型，会把三类不同物理系统混在一起。

### 6.3 64G、96G、128G 的影响不是只改 OOM 阈值

每 rank 必须满足：

```text
M_rank =
    M_weight_shard
  + M_kv_and_compressor_state
  + M_activation_and_workspace
  + M_graph_capture
  + M_comm_buffers
  + M_allocator_fragmentation
  + M_safety_margin
  <= C_card × utilization
```

显存变化会触发：

- 最小 TP/EP/PP 改变；
- 可捕获图桶数量改变；
- KV block 数、max context、max batch 改变；
- Prefix Cache 命中/淘汰分布改变；
- chunked prefill 阈值和调度 token budget 改变；
- 是否使用 KV quant、CPU/NVMe offload、重算改变；
- EPLB redundant expert 数改变；
- workspace 不足时 Kernel/fusion 回退。

因此建议先由 `PlacementCompiler` 枚举合法并行方案，再分别建立执行域，而不是让“显存大小”直接进入算子耗时公式。

### 6.4 拓扑决定通信路径

HCCL 会根据 server 内/跨 server 拓扑、消息大小、硬件资源和 rank 数选择 Mesh、Ring、RHD、NHR、AHC 等不同算法；相同 AllReduce/AllToAll 名称并不代表相同通信路径。[HCCL 集合通信算法说明](https://www.hiascend.com/document/detail/en/canncommercial/850/commlib/hcclug/hcclug_000115.html)

通信指纹至少应包含：

```text
group_members_and_order
rank_to_node_and_link
collective_type
algorithm
payload_per_rank / send_counts / recv_counts
dtype_and_layout
chunk/pipeline count
stream and overlap interval
cross_node / cross_supernode
```

性能可以用 α-β-γ 模型做先验，但 α、β、γ 和算法必须按实际拓扑校准，不能从 8 卡线性放大到 128 卡。

## 7. “没做好泛化”除了走错控制路线还会发生什么

| 失败模式 | 糟糕结果 | 检测方法 | 处理原则 |
|---|---|---|---|
| 逻辑 shape 对、物理 shape 错 | Kernel 变、内存/耗时错，甚至读取错误 packed 数据 | 对比 layout/stride/format/scale/pad | 物理 descriptor 进入签名 |
| TP/EP 整除或复制规则错 | local head/expert shape 错、collective 错 | 启动前约束求解 + rank trace | 非法组合拒绝执行 |
| 量化误差改变 route/TopK | expert token histogram、稀疏 block、通信全变 | 保存 TopK margin 与离散结果 diff | 量化组合必须从上游真实执行 |
| 卡数改变了并行方案 | 把 DP 扩容误当 TP 缩放 | 比较完整 rank-group 图 | 以 ParallelPlan 而非 world size 建域 |
| 显存改变 scheduler/cache 路径 | 同请求变成 chunk、evict、offload、recompute | 采集水位、block 事件、调度决策 | 状态与容量进入指纹 |
| 图桶/Guard 未记录 | 新 shape 重编译或 fallback，性能结论失真 | graph ID、bucket、Guard、cache hit | cold/warm 与每个图桶分档 |
| Kernel/Tiling 变了 | 同 op/shape 仍有显著性能差 | Kernel、TilingKey、blockDim、workspace | 跨 TilingKey 不复用 |
| MoE 只保存均值 | 热点 expert 和尾部 rank 被掩盖 | per-layer histogram、max/mean、peer counts | 保存 ragged offsets 和分布 |
| EPLB 动态 remap 未记录 | 同 route 发送到不同 rank，通信不同 | placement epoch/map hash | EPLB epoch 进入状态路径 |
| Cache 状态伪造 | prefix hit、partial compressor state 不真实 | 从根输入演进并核对事件 | 状态机生成优先于 tensor 注入 |
| Stub 注入分支结果 | 下游能跑但绕过决定路径的计算 | provenance 与 `path_tainted` | 污染样本禁止性能验收 |
| batch 非确定性 | 同请求因 batch 顺序产生不同输出/route | batch composition 对照 | Golden 与生产性能样本分开 |
| 多模态预处理漂移 | 视觉 token 数和 mask 全变 | 保存 processor/config/revision/grid | 预处理属于模型执行域 |
| checkpoint 配置漂移 | “同模型不同量化”实际 MTP/ignored layers 不同 | config/weight/chat template 全量 hash/diff | checkpoint revision 是一级键 |
| 后端无优化实现而 fallback | 功能正确但性能完全不代表目标 | backend/kernel implementation ID | fallback 单独建域并降置信度 |
| 跨路径平均性能 | 产生 Simpson 悖论式错误结论 | 按 PathFingerprint 分层统计 | 先路径分档，再按 workload 概率加权 |

此外，vLLM 默认并不保证跨硬件、跨版本可复现；即使固定 seed，也只在相同硬件和相同 vLLM 版本下提供有限保证。[vLLM Reproducibility](https://docs.vllm.ai/en/latest/usage/reproducibility/)

Ascend batch invariance 会切换确定性 Kernel，并与部分 full graph 模式不兼容且可能牺牲性能。因此“确定性 Golden”与“生产性能模式”必须是两个独立执行域。[vLLM Ascend Batch Invariance](https://docs.vllm.ai/projects/ascend/en/main/user_guide/feature_guide/batch_invariance.html)

## 8. 推荐的泛化框架

### 8.1 ExecutionSpec：录制前先规范化场景

建议新增以下规范化对象：

```yaml
execution_spec:
  model:
    family: deepseek_v4
    checkpoint: deepseek-ai/DeepSeek-V4-Pro
    revision: "<commit>"
    config_hash: "<sha256>"
    weight_manifest_hash: "<sha256>"
    tokenizer_hash: "<sha256>"
    model_adapter_version: "deepseek_v4@0.1"

  quantization:
    weight: fp4_expert_fp8_other
    activation: dynamic_fp8
    kv_cache: fp8
    index_cache: fp4
    block_shape: [128, 128]
    ignored_layers_hash: "<sha256>"

  parallel_plan:
    tp: 8
    dp: 8
    pp: 1
    ep_enabled: true
    dcp: 1
    rank_group_graph_hash: "<sha256>"
    placement_hash: "<sha256>"

  hardware:
    nodes: 8
    devices_per_node: 8
    hbm_bytes_per_device: 68719476736
    soc: "<exact>"
    topology_hash: "<sha256>"

  workload:
    phase: prefill
    request_seq_lens: [4095, 4096]
    scheduled_tokens: 8192
    batch_composition_hash: "<sha256>"
    multimodal_manifest: null

  state:
    kv_block_size: 128
    prefix_hit_blocks: 0
    compressor_state_hash: "<sha256>"
    cache_watermark_bucket: low
    eplb_epoch: 0

  software:
    framework: vllm
    framework_revision: "<commit>"
    backend: vllm-ascend
    backend_revision: "<commit>"
    cann: "<version>"
    driver: "<version>"
    graph_mode: full_decode_only
    feature_flags_hash: "<sha256>"
```

任何关键字段未知时都应显式为 `unknown`，而不是省略后使用默认值。

### 8.2 ModelAdapter：把模型差异限制在小接口内

公共框架不应硬编码每个模型。建议每个架构实现：

```text
ModelAdapter
  parse_and_validate_config()
  enumerate_layer_kinds()
  build_symbolic_shape_graph()
  build_sharding_constraints()
  describe_quantized_physical_layout()
  enumerate_branch_catalog()
  build_runtime_state_machine()
  identify_path_critical_values()
  define_numeric_and_discrete_oracles()
  normalize_backend_observations()
```

首批 Adapter：

1. `deepseek_v4`：覆盖 Pro/Flash，共享公共逻辑，但 layer schedule、TopK、H/E 等从 config 读取；
2. `glm_moe_dsa_5_2`：覆盖 dense/MoE 分界和 IndexShare；
3. `minimax_m3`：覆盖 MSA、MoE、多模态；
4. `qwen3_8`：暂为 `api_only` 占位；开放 config 后再从 Qwen3.5/3.6 Adapter 分叉验证。

### 8.3 Shape IR：必须表达不规则和条件 shape

IR 至少支持：

- 常量、符号、精确除法、上取整、对齐；
- piecewise/Guard；
- ragged list、segment offsets、histogram；
- sparse index、CSR/block table；
- quant pack/scale/zero-point；
- shard/replicate/collective payload；
- stateful recurrence；
- logical→physical→execution shape 三段映射。

每个 Tensor descriptor 建议为：

```text
TensorDescriptor
  semantic_role
  logical_shape_expr
  local_shape_expr
  physical_shape_expr
  execution_shape_expr
  dtype / quant / layout / stride / format
  shard_spec
  padding_spec
  state_scope
  value_semantics
  provenance
```

### 8.4 路径谓词来源

每条路径的进入条件不能只靠人工写。建议合并四类证据：

1. **静态配置/源码**：layer kind、`if`、feature flag、支持矩阵；
2. **编译证据**：Guard、graph break、bucket、fusion；
3. **动态差分**：一次只改变一个参数/值/状态，观察首个指纹差异；
4. **Kernel/Tiling 反推**：根据 TilingKey、workspace、Kernel 变化定位未知边界。

PyTorch 对依赖 shape 或数据值的分支会使用 hint/Guard；条件变化可能重编译或发生 data-dependent shape 错误。因此 Guard 本身就是路径谓词证据，而不是编译器内部噪声。[PyTorch 动态 shape/数据依赖 Guard 说明](https://docs.pytorch.org/docs/stable/user_guide/torch_compiler/compile/dynamic_shapes_troubleshooting_guardon_errors.html)

### 8.5 面向路径的值与状态生成

优先级建议：

1. 真实 token/image/video 根输入；
2. 真实 Prefill→Decode 状态演进；
3. 合法的并行重分片和量化；
4. 长度、padding、block table、batch composition 的规则生成；
5. 针对分支谓词的约束求解；
6. 围绕已知样本的边界/差分变异；
7. 仅对不影响关键路径的连续 Tensor 做统计合成。

特别规则：

- 对每个量化方案重新执行 router、indexer、sampling 和 MTP 验证，不能注入 BF16 的离散决策冒充量化路径；
- 对 cache/compressor state 优先从根输入演进；
- 如果不得不注入 route、TopK、cache table 等值，标记 `path_tainted=true`，仅用于局部功能测试。

### 8.6 覆盖策略：以路径和边界为目标

不要做所有参数笛卡尔积。推荐顺序：

1. 每模型/量化/并行方案一个 Golden；
2. 单因素差分；
3. 每个路径谓词的 MC/DC 或两侧覆盖；
4. block、tile、quant group、graph bucket、TP/EP 整除边界的 `-1/0/+1`；
5. pairwise/3-wise covering array；
6. 高风险组合：量化×MoE 偏斜、长上下文×低显存、跨机 TP×小消息、MTP×cache；
7. 根据新 PathFingerprint 做主动学习；
8. 用保留组合验证未知检测能力。

停止条件不是“参数都跑完”，而是：

- 新路径发现率趋于饱和；
- 已知路径边界稳定；
- 保留集上路径预测和 unknown 检测达到目标；
- 真机补测主要随新路径数而非组合总数增长。

### 8.7 PathFingerprint V2

仓库已有 V1 七层指纹。建议在不破坏原结构的前提下扩展：

```text
model_path
  + checkpoint/config/adapter revision
  + layer kind schedule
  + discrete decisions: route/index/MTP/EOS

graph_path
  + actual/padded shapes
  + graph bucket/Guard/recompile/fallback

kernel_path
  + logical/local/physical/execution descriptors
  + quant kernel/layout/scales
  + Kernel/TilingKey/blockDim/workspace/fusion

runtime_path
  + batch composition/chunk/PD role
  + capacity/offload/recompute

state_path
  + KV/compressor/index/prefix cache
  + block events and watermarks
  + EPLB epoch/map

communication_path
  + rank-group graph/topology
  + collective algorithm
  + per-peer counts and overlap

environment
  + hardware capacity
  + complete feature flags
  + software/checkpoint hashes
```

Ascend C 的 TilingData、TilingKey、blockDim 和 workspace 会选择不同实现；TilingKey 明确定义为区分 Kernel 专用实现的键，因此必须进入指纹。[Ascend C Tiling 概念](https://www.hiascend.com/document/detail/en/canncommercial/850/opdevg/Ascendcopdevg/atlas_ascendc_10_00013.html)

### 8.8 执行域等价等级

| 等级 | 条件 | 可做什么 |
|---|---|---|
| Exact | ExecutionSpec 与严格指纹一致 | 直接比较数值和性能 |
| Path-equivalent | ID 可不同，但层/图/Kernel/Tiling/状态/通信语义经真机证明等价 | 同域建模、插值 |
| Shape-equivalent only | shape 相同但路径字段不同/未知 | 只能做功能参考 |
| Structural-transfer | 复用公共公式/Adapter 经验，但模型/量化/拓扑不同 | 推荐实验点，不复用耗时 |
| Unknown | 超边界、新 Kernel、新通信/状态路径 | 必须补测或返回 unavailable |

自动系统只能把样本降级，不能在没有证据时自动升级到 Path-equivalent。

## 9. 第一版实验矩阵

### 9.1 分阶段而不是全组合

| 阶段 | 目标 | 模型/配置 | 主要输出 |
|---|---|---|---|
| S0 静态事实抽取 | 验证 Adapter 能从 config 得到层类型、约束和符号 shape | DS-V4 Pro/Flash、GLM-5.2、M3 BF16/MXFP8；Qwen3.8 api-only | ConfigDiff、LayerMap、ShapeGraph |
| S1 单节点路径边界 | 找到同 shape 不同路径、边界两侧和量化离散决策变化 | 优先 DS-V4-Flash W8A8、GLM-5.2 W4A8C8；M3 在支持硬件 | PathFingerprint、branch catalog |
| S2 规模/显存方案切换 | 区分 DP 扩容、TP/EP 重分解、跨机通信和容量路径 | 8 卡、64 卡、128 卡；64/96/128G 可用环境 | ParallelPlan diff、通信/内存域 |
| S3 跨模型迁移 | 验证公共 IR 与 Adapter 边界 | Pro、GLM、M3 作为保留模型/配置 | 预测准确率、unknown 检出率 |
| S4 持续补充 | 跟随 checkpoint/backend 更新 | Qwen3.8 开放后、MiniMax-M3 Ascend 支持后 | 新 Adapter/回归 |

### 9.2 模型与量化锚点

| 模型 | 首批锚点 | 原因 |
|---|---|---|
| DeepSeek-V4-Flash | 官方/Ascend W8A8；必要时原生 mixed FP4/FP8 | 单节点可运行，且包含 C4/C128/SWA、MoE、MTP，路径丰富 |
| DeepSeek-V4-Pro | W4A8 + 16/32 卡起步，64 卡规模验证 | 用于验证同族跨尺寸、跨节点与高 expert 数 |
| GLM-5.2 | BF16、W8A8、W4A8C8 至少两种 | 官方 Ascend 文档完整，IndexShare 是关键新路径 |
| MiniMax-M3 | BF16 与官方 MXFP8 config；先在 vLLM 已验证硬件 | 验证 MSA、多模态和 checkpoint 配置漂移 |
| Qwen3.8 | 仅状态监控/API 行为 | 等待开放 config，不伪造结构 |

DeepSeek-V4-Flash 的官方 Ascend 文档说明 W8A8+MTP 可在单个 `64G × 8` A2 或 `128G × 8` A3 节点部署；Pro W4A8+MTP 至少需要 4 个 `64G × 8` A2 或 2 个 `128G × 8` A3 节点。这两个配置很适合验证“同族模型尺寸→容量→并行方案→路径”的因果链。[Flash 部署](https://docs.vllm.ai/projects/ascend/en/main/tutorials/models/DeepSeek-V4-Flash.html)、[Pro 部署](https://docs.vllm.ai/projects/ascend/en/latest/tutorials/models/DeepSeek-V4-Pro.html)

### 9.3 首批边界点

#### 通用

- `T/B/S`：1、图桶边界、max batched tokens 前/等于/后；
- KV block：`b-1/b/b+1`；
- quant group/tile：`G-1/G/G+1`；
- TP/EP：合法最小值、单机上限、第一次跨机；
- 显存：刚好不触发/触发 chunk、evict、offload、重算；
- batch：同请求单独、同长度混合、长短混合、到达顺序交换。

#### DeepSeek-V4

- position：`4k-1/4k/4k+1`，`128k-1/128k/128k+1`；
- C4A/C128A/SWA 层各至少一层；
- compressed entries 小于/等于/大于 index TopK；
- IndexCache off/on、compute/reuse；
- MTP accept 0/1/多 token；
- route 均匀/热点/极端偏斜。

#### GLM-5.2

- dense 第 3 层与首个 MoE 层；
- IndexShare full 层与 3 个 shared 层；
- `index_topk=2048` 有效长度边界；
- `enable_dsa_cp`、C8 index/SFA cache 开关；
- W4A8C8 与 W8A8 的 route/TopK margin 对照；
- MTP accepted length 分档。

#### MiniMax-M3

- 前 3 层与首个 sparse/MoE 层；
- KV blocks：15/16/17 及更长上下文；
- text only、1 image、多图、短视频；
- 动态分辨率网格边界；
- BF16/MXFP8 config 与 MTP module 数差异；
- Top-4 expert 路由 margin 小/大的 token。

### 9.4 64 卡/128 卡建议对照

至少设计三组，而不是只做一次扩容：

1. **DP 扩容对照**：固定单副本 TP/EP，64→128 只增 DP；
2. **TP 跨机对照**：固定全局 workload，TP=8→16，验证 local shape 与 HCCL 路径；
3. **容量/调度对照**：固定并行方案，分别限制可用 HBM 为 64G/96G 桶，观察 KV、graph、chunk/offload 路径。

如果物理上没有 96G 卡，可先用显存配额构造容量边界实验，但报告必须标记为 `capacity-capped`，不能声称等价于真实 96G 型号，因为带宽、算力、互联和 Kernel 支持可能不同。

## 10. 采集、验证与性能建模

### 10.1 最小采集字段

```text
Identity
  execution_spec_hash
  config/weight/tokenizer/adapter/software revision

Shape
  logical/local/physical/execution shapes
  actual/padded/ragged offsets
  quant scale/pack layout

Decision
  branch operands/outcomes
  MoE route histogram + margin
  sparse TopK indices/count + margin
  MTP proposals/accepted count
  EOS/stop reason

State
  KV/compressor/index/prefix cache
  alloc/free/evict/offload/recompute
  scheduler batch/chunk
  EPLB placement epoch

GraphKernel
  graph/bucket/Guard/cache/fallback
  op/kernel/TilingKey/blockDim/workspace/fusion

Communication
  group/topology/collective/algorithm
  payload/send_counts/recv_counts/stream/overlap

Performance
  cold compile/load
  warm device p50/p90/p99
  compute/communication/overlap
  peak HBM/workspace
  end-to-end/RPC
```

### 10.2 四重验证

```text
numeric_match
discrete_decision_match
state_transition_match
path_match
```

建议输出：

```text
numeric_match            = pass/fail/not_applicable
discrete_decision_match  = exact/mismatch/unknown
state_transition_match   = exact/equivalent/mismatch/unknown
path_match               = exact/equivalent/approximate/mismatch/unknown
performance_evidence     = measured/interpolated/extrapolated/unavailable
path_tainted             = true/false
```

判定顺序：

1. 先检查 ExecutionSpec 与输入/状态 provenance；
2. 再检查离散决策和状态转移；
3. 再检查分层 PathFingerprint；
4. 只有 exact/equivalent 才消费性能样本；
5. 最后检查连续数值与统计波动。

### 10.3 性能模型

单路径成本可拆为：

```text
Cost(regime) =
    Compute(kernel_shapes, kernel_ids)
  + Communication(group, algorithm, payloads)
  - Overlap(stream_timeline)
  + SchedulerAndRPC
```

目标 workload：

```text
ExpectedCost =
  Σ Probability(regime_i | workload, capacity, topology)
    × Cost(regime_i)
```
1
关键不是公式形式，而是 `Probability` 不能由单次 Golden Run 代替。它需要真实或明确假设的：

- sequence/batch 分布；
- route/expert 热度；
- prefix/cache 命中率；
- MTP 接受分布；
- 请求到达过程；
- memory watermark/offload 概率；
- 并行方案和拓扑。

同一执行域内可以对连续 `T/S/payload` 插值；跨 layer kind、quant kernel、TilingKey、graph bucket、collective algorithm、cache/offload 状态时必须切换模型或补测。

## 11. 第一版验收指标

这些是研究目标，不是当前已实现结果。

### 11.1 静态与 shape

- 支持的 checkpoint 全量 config/weight/tokenizer revision 可追溯；
- Adapter 对每个主层和 MTP/vision 模块的类型映射无遗漏；
- 已支持算子的逻辑/local/physical shape 与真实 trace 精确一致；
- 非法 TP/EP/CP/quant 组合在执行前被拒绝；
- config 漂移自动产生可读 diff。

### 11.2 路径

- 首批关键分支具备稳定 branch ID；
- 重复 Golden Run 的等价指纹稳定；
- 每个 mismatch 能定位到第一个差异层；
- route/TopK/MTP/cache 等关键离散决策可精确比较；
- 未见组合不会静默套用邻近 shape 的性能。

### 11.3 泛化

- 保留一组未参与建库的模型×量化×并行×边界组合；
- 路径预测报告置信度和证据来源；
- 新路径能够被识别为 unknown 并进入补测队列；
- 真机实验数主要随新执行域数量增长，而不是随参数笛卡尔积增长；
- 跨模型迁移只复用 Shape IR/Adapter 规则，不无证据复用耗时。

### 11.4 性能

- cold/warm、device/RPC、compute/communication/overlap 分离；
- exact/equivalent 域内误差按 p50/p90/p99 和置信区间报告；
- 跨 TilingKey、通信算法或状态路径自动降级为 extrapolated/unavailable；
- 64→128 卡报告能解释是 DP、TP/EP、CP/PP 或 PD 哪种变化。

## 12. 推荐实施顺序

### 12.1 第一步：先升级数据模型

在扩大实验之前：

- 为现有 PathFingerprint schema 增加 ExecutionSpec 引用；
- 增加 logical/local/physical/execution shape；
- 增加 quant、rank-group/topology、cache/EPLB epoch；
- 将 checkpoint/config/tokenizer 和所有 feature flags 做 hash；
- 支持 `unknown` 和 `path_tainted`。

### 12.2 第二步：做三个 Adapter 的静态版

不依赖大规模硬件即可完成：

- 读取官方 config；
- 输出 LayerMap；
- 输出 TP/EP 合法性约束；
- 输出候选 shape 图；
- 输出关键 branch catalog；
- 对 BF16/量化 checkpoint 做 config diff。

优先顺序：

1. DeepSeek-V4；
2. GLM-5.2；
3. MiniMax-M3；
4. Qwen3.8 等开放 config。

### 12.3 第三步：用 DeepSeek-V4-Flash 和 GLM-5.2 建真机闭环

两者已有较完整的 vLLM Ascend 支持/部署资料，适合验证：

- compressor/DSA/IndexShare；
- MoE route 与 EP；
- W8A8/W4A8C8；
- graph/Kernel/Tiling；
- 单机与多机；
- MTP 与 cache。

DeepSeek-V4 在 vLLM Ascend 已包含 DSA、多流 overlap、MTP、混合 KV、IndexCache 等优化，这些开关本身应成为不同执行域，而不是“同模型的环境细节”。[vLLM Ascend Release Notes](https://docs.vllm.ai/projects/ascend/en/latest/user_guide/release_notes.html)

### 12.4 第四步：用 Pro、M3 和 64/128 卡做保留验证

- Pro 验证同族跨规模；
- M3 验证跨架构、多模态和 MXFP8；
- 64/128 卡验证并行方案编译与通信；
- 96G 用真实卡或显存配额做容量敏感性，但严格区分证据等级。

## 13. 仍待验证的问题

1. DeepSeek-V4 各目标昇腾版本的 C4A/C128A/SWA 具体 TilingKey 边界；
2. Pro/Flash 在 64/96/128G 条件下最优 TP/DP/EP/CP 方案是否稳定；
3. W4A8/W8A8 对 MoE route、DSA TopK margin 和 MTP 接受率的影响分布；
4. GLM-5.2 IndexShare 在不同 context、MTP iteration 和量化下的 reuse/compute 真实路径；
5. C8 index cache/SFA cache 对物理 shape、精度和 Kernel 的独立影响；
6. MiniMax-M3 BF16 与 MXFP8 `num_mtp_modules` 差异的官方版本语义；
7. MiniMax-M3 在昇腾上的 MSA、多模态、MTP 和量化支持计划；
8. Qwen3.8 开放权重/config 的时间和最终结构；
9. 目标 96G 卡的具体型号、算力、带宽、互联和 CANN Kernel 支持；
10. EPLB 动态 remap 是否纳入性能测试，采用动态还是固定 recorded map；
11. Prefill/Decode 分离下，不同 P/D TP/DP 与 KV transfer layout 的等价条件；
12. 从推理扩展训练时，optimizer/ZeRO/重算和数值 overflow 如何进入同一 IR。

## 14. 最终建议

第一版不应追求一个“输入参数量、量化 bit、卡数就直接输出所有 shape 和耗时”的总公式。更可行且可验证的路线是：

1. 以 `ExecutionSpec` 固定事实和版本；
2. 以 `ModelAdapter + Symbolic Shape IR` 推导逻辑、局部和物理 shape；
3. 以路径谓词和值/状态生成主动触发目标路径；
4. 以真实硬件的分层 `PathFingerprint` 校正和验收；
5. 以执行域等价类压缩实验数量；
6. 对未知路径诚实返回 unknown，并自动补测；
7. 只在同路径、同 Kernel/Tiling、同状态和同通信语义内复用性能。

一句话总结：

> **泛化的目标不是让新参数组合“能跑”，而是让系统能够从模型、量化、并行、容量、输入值和状态中推导候选执行域，在真机上识别实际路径，并只对有充分路径证据的样本给出性能结论。**

## 15. 主要参考资料

### 模型与配置

- [DeepSeek-V4-Pro 模型卡](https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro)
- [DeepSeek-V4-Pro config](https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro/raw/main/config.json)
- [DeepSeek-V4-Flash config](https://huggingface.co/deepseek-ai/DeepSeek-V4-Flash/raw/main/config.json)
- [GLM-5.2 模型卡](https://huggingface.co/zai-org/GLM-5.2)
- [GLM-5.2 config](https://huggingface.co/zai-org/GLM-5.2/raw/main/config.json)
- [MiniMax-M3 模型卡](https://huggingface.co/MiniMaxAI/MiniMax-M3)
- [MiniMax-M3 BF16 config](https://huggingface.co/MiniMaxAI/MiniMax-M3/raw/main/config.json)
- [MiniMax-M3 MXFP8 config](https://huggingface.co/MiniMaxAI/MiniMax-M3-MXFP8/blob/main/config.json)
- [QwenCloud 官方模型列表](https://docs.qwencloud.com/developer-guides/getting-started/text-generation-models)

### 推理实现与部署

- [vLLM DeepSeek-V4 长上下文实现说明](https://github.com/vllm-project/vllm-project.github.io/blob/main/_posts/2026-04-24-deepseek-v4.md)
- [vLLM DeepSeek-V4 attention 实现](https://docs.vllm.ai/en/latest/api/vllm/models/deepseek_v4/attention/)
- [vLLM DeepSeek-V4 compressor 实现](https://docs.vllm.ai/en/latest/api/vllm/models/deepseek_v4/compressor/)
- [vLLM MiniMax-M3 sparse metadata](https://docs.vllm.ai/en/latest/api/vllm/models/minimax_m3/common/sparse_attention/)
- [vLLM MiniMax-M3 recipe](https://recipes.vllm.ai/MiniMaxAI/MiniMax-M3)
- [vLLM Ascend DeepSeek-V4-Flash](https://docs.vllm.ai/projects/ascend/en/main/tutorials/models/DeepSeek-V4-Flash.html)
- [vLLM Ascend DeepSeek-V4-Pro](https://docs.vllm.ai/projects/ascend/en/latest/tutorials/models/DeepSeek-V4-Pro.html)
- [vLLM Ascend GLM-5.2](https://docs.vllm.ai/projects/ascend/en/latest/tutorials/models/GLM5.2.html)
- [vLLM Ascend 支持矩阵](https://docs.vllm.ai/projects/ascend/en/latest/user_guide/support_matrix/supported_models.html)

### 泛化、并行、图、量化与通信

- [PyTorch 数据依赖动态 shape/Guard](https://docs.pytorch.org/docs/stable/user_guide/torch_compiler/compile/dynamic_shapes_troubleshooting_guardon_errors.html)
- [vLLM Quantization](https://docs.vllm.ai/en/stable/features/quantization/index.html)
- [vLLM Data Parallel Deployment](https://docs.vllm.ai/en/v0.12.0/serving/data_parallel_deployment/)
- [vLLM Expert Parallel Deployment/EPLB](https://docs.vllm.ai/en/stable/serving/expert_parallel_deployment/)
- [vLLM Ascend EPLB](https://docs.vllm.ai/projects/ascend/en/latest/user_guide/feature_guide/expert_parallelism_load_balancer.html)
- [vLLM Ascend Graph Mode](https://docs.vllm.ai/projects/ascend/en/main/user_guide/feature_guide/graph_mode.html)
- [vLLM Ascend Context Parallel](https://docs.vllm.ai/projects/ascend/en/latest/user_guide/feature_guide/context_parallel.html)
- [vLLM Ascend Batch Invariance](https://docs.vllm.ai/projects/ascend/en/main/user_guide/feature_guide/batch_invariance.html)
- [Ascend C TilingKey/TilingData](https://www.hiascend.com/document/detail/en/canncommercial/850/opdevg/Ascendcopdevg/atlas_ascendc_10_00013.html)
- [HCCL 集合通信算法与 α-β-γ 模型](https://www.hiascend.com/document/detail/en/canncommercial/850/commlib/hcclug/hcclug_000115.html)
